<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unysoft.bimmik">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mahasiswa.Register" />
        <activity
            android:name=".mahasiswa.Dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme" />
        <activity android:name=".mahasiswa.InputNilaiSemester" />
        <activity android:name=".mahasiswa.Profile" />
        <activity android:name=".mahasiswa.GetNilaiSemester" />
        <activity android:name=".mahasiswa.Kegiatan" />
        <activity android:name=".mahasiswa.Chat" />
        <activity android:name=".dosen.Dosen_dashboard" />
        <activity
            android:parentActivityName=".dosen.Dosen_dashboard"
            android:name=".dosen.Dosen_detail_mhs"
            android:theme="@style/adaToolbar"
            android:label="Detail mahasiswa" />
        <activity android:name=".dosen.Dosen_profile" />
        <activity android:name=".dosen.Dosen_chat"></activity>
    </application>

</manifest>